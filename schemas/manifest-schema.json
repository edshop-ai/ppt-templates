{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Slide Template Layout Manifest",
  "type": "object",
  "required": ["template_id", "elements", "colors", "fonts"],
  "properties": {
    "template_id": {
      "type": "string",
      "description": "grade_band__subject_or_track__archetype__vN"
    },
    "elements": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "role", "left", "top", "width", "height"],
        "properties": {
          "id": { "type": "string" },
          "role": {
            "type": "string",
            "enum": ["title", "subtitle", "body_text", "bullet", "image", "label", "shape", "table"]
          },
          "left": { "type": "number" },
          "top": { "type": "number" },
          "width": { "type": "number" },
          "height": { "type": "number" },
          "font": { "type": "string" },
          "font_size_pt": { "type": "number" },
          "color_hex": { "type": "string" },
          "max_chars": { "type": "number" },
          "shape_type": {
            "type": "string",
            "enum": ["rect", "rounded_rect", "circle", "line", "arrow", "pill"],
            "description": "Required when role is 'shape'. 'arrow' must be reported as its own element, never merged with an adjacent line — PowerPoint has a native arrow connector shape and the conversion script needs to target it directly."
          },
          "corner_radius": {
            "type": "number",
            "description": "px, only for rounded_rect/pill shapes"
          }
        }
      }
    },
    "colors": {
      "type": "object",
      "required": ["accent", "background", "text_primary", "text_secondary"],
      "properties": {
        "accent": { "type": "string" },
        "background": { "type": "string" },
        "text_primary": { "type": "string" },
        "text_secondary": { "type": "string" }
      }
    },
    "fonts": {
      "type": "object",
      "required": ["heading", "body"],
      "properties": {
        "heading": { "type": "string" },
        "body": { "type": "string" }
      }
    }
  }
}
